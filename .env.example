# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret-here

# Slack App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Allowed Channels (comma-separated channel IDs, leave empty to allow all channels)
# Example: ALLOWED_CHANNELS=C01234567,C09876543
ALLOWED_CHANNELS=

# Environments (comma-separated environment names, defaults to staging,dev)
# Example: ENVIRONMENTS=staging,dev,qa,production
ENVIRONMENTS=staging,dev

# Services (comma-separated service names)
# Example: SERVICES=fundraisers-api,fundraisers-worker,group-leaders
SERVICES=fundraisers-api,fundraisers-internal-tools,fundraisers-pdf-worker,fundraisers-redis-consumer-worker,fundraisers-subscription-api,fundraisers-sync-worker,fundraisers-worker,group-leaders,group-leaders-api,participants-api

# Database Path (optional, defaults to ./environments.db)
# For Railway with volume: DB_PATH=/data/environments.db
DB_PATH=./environments.db
